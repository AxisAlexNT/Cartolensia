# Migrations

SQL migrations for the PostgreSQL-backed core live here.

The first implementation run should add ordered migration files for the schema described in `docs/DB_SCHEMA.md` and embed them in the Go backend.
